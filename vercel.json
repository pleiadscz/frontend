{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"framework": null,
	"buildCommand": "npm run build",
	"outputDirectory": "build",
	"installCommand": "rm -f pnpm-workspace.yaml && npm install",
	"rewrites": [
		{
			"source": "/((?!.*\\.).*)",
			"destination": "/index.html"
		}
	]
}
